@database MultiRename
@author Uwe Rosner
@wordwrap
@node Main "MultiRename - Introduction"

@{fg shine}MultiRename 1.0 released on 01.10.2025.@{fg text}

@{b}About@{ub}

MultiRename is a tool to rename the contents (files and directories) within one directory according to a specified mask. It is inspired by the MultiRename tool of the Windows tool TotalCommander, but it does not have all of its features.

Many thanks to Mason for creating the beautiful color icon for MultiRename in AmigaOS 3.2 style!

If you encounter any problems with this tool, please feel free to send me a brief report, Uwe Rosner (u.rosner@ymail.com).

@{"General usage" link usage}
@{"The masks" link masks}
@{"Menus and functions" link menus}
@{"Using MultiRename with DOpus 5.x" link dopus}
@{"Arguments and tool types" link arguments}
@{"Changelog" link changelog}
@{"Distribution" link distribution}

@endnode

@node usage "How to use"

@{b}Installation@{ub}
Just copy the binary MultiRename together with its icon to a place of your choice, e.g. to @{fg shine}SYS:Tools@{fg text}.

@{b}Usage@{ub}

After MultiRename is started, first add some files: either by using menu 'Project' -> 'Add files...' or by dragging them into the processing list. Its also possible to start MultiRename directly with files as parameters, see chapter @{"Arguments and tool types" link arguments}.

A default mask has already been defined in both, the name field and the extension field. The buttons below the mask fields help you to set your mask masks.

@{b}All source files must be within the same directory@{ub}

When MultiRename adds the first source file to the processing list, it sets this files location as 'working directory' which then is shown in the window title. All other added files must be located in this directory - or an error requester appears.

@{b}The error requester@{ub}
Whenever something goes wrong, an error requester is shown. It gives you the choice of clicking 'Ok' to continue working or clicking on 'Show errors' will open a window showing you what happened and which files are affected.

@{i}In this first version, the output is only displayed in a simple CLI window, which cannot even be closed while the app is running.@{ui}

@{b}Icon handling@{ub}
Note that Icons are never visible in the MultiRename's processing list, even in icons processing mode not. Instead the behavior in this mode is that for each file in the list there is a check if a .info file exists for it, and if so, this .info file is renamed, accordingly.

@{b}The processing list@{ub}

There are four columns in the processing list:

- State: Should show 'Ok' for all files so that 'Start rename' can be clicked to begin renaming. Other values: 'Cmd' - error in the pattern. '> 27', '> 32', '> 102', '> 107' - depending on whether the icon processing mode is active or not and whether long file names are allowed, these numbers indicate that the resulting file name in this line is longer than allowed.

- Type: In this first version it only shows 'DIR' for directories and stays empty for normal files.

- Old/new name: The old and te new name of each source file.

@{b}Start rename@{ub}

When all all files have an 'State' of 'Ok' you can start the rename operation by clicking on 'Start rename'

@{b}Special note@{ub}

File names ending with one or multiple dot '.' characters are not generated by this tool. If you create such a mask the trailing dots don't appear in the new name field.

@endnode

@node masks "The masks"

At the top of the MultiRename window are the input fields for the name masks. The following functions can be applied using the masks:

- Create a numbered file list 
- Convert to upper/lower case letters 
- Replace a specific text in the file name with another one
- Inserting the date/time of the file 
- Keep only certain letters of the file name 
- Mix all the masks above
- All of the above for name extension, too

@{b}File name mask@{ub}

In this field you can enter a definition for the new file name. The buttons below allow you to insert placeholders for the old name, parts of it, a counter or file date/time. Placeholders are always enclosed in square brackets [], while all other characters (without the square brackets) are transferred to the new name without change Example: [C][N2-8] numbers the files and takes over the letters 2 - 8 of the old name. A detailed description of all available placeholders can be found in the help. 

@{b}Extension mask@{ub}

Here you can change the file extension. In general, all placeholders can be added to each of the two input fields. MultiRename generates the renaming string as follows: Mask for file name + '.' + Mask for extension. The reason for two separate fields is to avoid accidentally removing the file extensions. 

@{b}Part selection window@{ub}

When clicking the '[N#-#]' or '[E#-#]' button, the part selection window opens. There you can select the portion of the name or extension you wanted to be inserted in the mask field at cursor position.

@{b}Define counter@{ub}

- Start at: Number of the first file. The files are always displayed in the order in which they appear in the processing list, numbered.
- Increment: the counter is incremented/decremented by this value. 
- Number of digits: width of the counter field.

@node menus "Menus and functions"

@{b}Project -> New@{ub}

With 'New' you can start a new session. The processing list will be cleared and the 'working directory' will be reset.

@{b}Project -> Add files@{ub}

'Add files...' open a file requester and let you select files to add to processing list. If the processing list already contains files, you will be warned if selected files are already in the list. And also if you want to add files from another source directory than the existing files.

@{b}Settings -> Allow long filenames@{ub}

Enables the long name mode.

The default maximum length of file names created by the tool is 32 characters, as this was the length supported on the Amiga from the beginning. If you use a file system with long file name support like PFS, SFS or the FFS v46+ (came with AmigaOS 3.1.4 onward) you can use the LONGNAMES tooltype / argument or the menu setting 'Allow long filenames' - which the allows 107 characters. 

Can be set with the LONGNAMES argument, see @{"Arguments and tool types" link arguments}.

@{b}Settings -> Skip icons@{ub}

Disables the icon processing mode.

Default behavior is that icons (.info files) are also processed. So, the allowed name length is reduced to 27 or 102 (in long name mode) characters. When 'Skip icons' is enabled, the full name length is available. This is 32 characters or 107 (in long name mode) characters. But with 'Skip icons' enabled, icon names are not processed and remain with their aoriginal names.

Can be set with the SKIPICONS argument, see @{"Arguments and tool types" link arguments}.

@endnode

@node dopus "Using MultiRename with DOpus 5.x"
If you use DOpus 5.x you can setup a button to run MultiRename from there:

1) Edit the file DOpus/ARexx/Compare.dopus5. Change the line starting with "Address Command", which is almost at the bottom, so that it looks like this:

@{fg shine}Address Command 'SYS:Tools/MultiRename "' || First_Name || '" "' || Second_Name || '"' IGNORELEADINGSPACES IGNORETRAILINGSPACES OLDERTOLEFT DONOTASK@{fg text}

Alternately, if you want to use SYS:Tools/TextEdit (Os 3.1.4 or later needed) and open it on the same screen as MultiRename runs on:

@{fg shine}Address Command 'DH0:Tools/MultiRename "' || First_Name || '" "' || Second_Name || '"' IGNORELEADINGSPACES IGNORETRAILINGSPACES OLDERTOLEFT DONOTASK EDITOR 'SYS:Tools/TextEdit' EDITORONPUBSCREEN@{fg text}

2) In DOpus create or edit a button, name it e.g. "Compare" and set this ARexx script to be executed on the button click:
@{fg shine}DOpus5:ARexx/Compare.dopus5 {Qp}@{fg text}

Now when clicking on "Compare" MultiRename will be started, immediately comparing the first two selected files. The files to compare can be selected from one lister or from two different listers.

@endnode

@node arguments "Arguments and tool types"

@{b}Files as CLI arguments@{ub}
The files to compare can be provided as command line arguments, simply calling 'MultiRename file1 file2'

@{b}Files as Workbench arguments@{ub}
The files also can be provided as Workbench arguments:
- Left-click one of the files that should be given as arguments
- Hold Shift and left-click the other files one by one
- Hold Shift and double-click MultiRename

@{b}CLI arguments, Workbench tool types@{ub}

For a better control of program behavior MultiRename supports some options which can be provided as CLI arguments or tool types of its Workbench icon.

@{i}LONGNAMES@{ui}
Enables the long name mode by enabling the setting 'Allow long filenames'. See @{"Menus and functions" link menus}.

@{i}SKIPICONS@{ui}
Disables the icons processing mode by enabling the 'Skip icons' setting. See @{"Menus and functions" link menus}.

@{i}PUBSCREEN@{ui}
The program will be opened on the provided public screen.

If this is not set MultiRename will open on the Workbench screen.

@endnode

@node changelog "Changelog"

@{b}Changelog@{ub}

1.0 (xx.10.2025)
- Initial version


@endnode


@node distribution "Author and distribution"

@{b}Author and distribution@{ub}

The author of MultiRename is Uwe Rosner (u.rosner@ymail.com).

This release of MultiRename may be freely distributed. It may not be commercially distributed without the explicit permission of the author.

@endnode

