# makefile for GNU make (automatically generated by makemake)
# NOTE: indent with TAB in GNU makefiles!

# paths are relative to the project directory (current directory during make)

EXE    = MultiRename_sas
OBJDIR = build-sas/

# compiler and linker

CC = sc:c/sc
LD = sc:c/sc

# target 'all' (default target)

all : $(EXE)

$(OBJDIR)application.o : src/application.c
	$(CC) nolink src/application.c

$(OBJDIR)arguments.o : src/arguments.c
	$(CC) nolink src/arguments.c

$(OBJDIR)main.o : src/main.c
	$(CC) nolink src/main.c

OBJS = $(OBJDIR)main.o $(OBJDIR)application.o $(OBJDIR)arguments.o

$(EXE) : $(OBJS)
	$(LD) link $(OBJS)

# target 'clean'

clean:
	-delete $(EXE)
	-delete $(EXE).info
	-delete $(EXE).lnk
	-delete $(OBJDIR)\*.o
